// Store famous person's name
const famousperson: string = "Albert Einstein";

// Famous quote
const _famousQuote: string = "A person who never made a mistake never tried anything new.";

// Compose the message
const $message: string = `${famousperson} once said, "${famous_Quote}"`;

// Print the message
console.log($message);
